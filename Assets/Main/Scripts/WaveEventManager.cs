using System.Collections;
using System.Collections.Generic;
using UnityEngine;


//used to manage the length of each level and allow other timer related components
//to trigger depending on when a level begins and ends.

//would be wasteful of my time If I started coding events since I don't 
//know what events are important to the game's design

public class WaveEventManager : MonoBehaviour {

    //all durations and delays are measured in seconds
    public float levelDuration;

	// Use this for initialization
	void Start () {
		
	}
	
	// Update is called once per frame
	void Update () {
		
	}
}
